syntax = "proto3";

package sessions;

service Sessions {
  rpc Create (AccessToken) returns (JWT) {}
  rpc Get (JWT) returns (JWT) {}
}

message JWT = {
  string jwt = 1;
};

message AccessToken {
  string accessToken = 1;
};
